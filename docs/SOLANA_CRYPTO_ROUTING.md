# Solana / Crypto Routing

DarwinAI is the active Solana PumpSwap trading organism lane.

Canonical cross-lane routing lives at `/Users/benjijmac/workspace-audits/SOLANA_CRYPTO_CONTROL_PLANE.md`. This repo keeps only local operator rules and implementation details.

Rules:

- Default to `DARWIN_MODE=paper`.
- Do not start, stop, or promote `darwin-live` without explicit operator intent.
- Treat Google Drive crypto code as archive/reference unless it is intentionally promoted into `/Users/benjijmac/Documents/Playground`.
- Map any Google Cloud runtime findings back to the control plane before changing infrastructure.
